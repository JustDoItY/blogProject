import { Component } from '@angular/core';
import { HeaderComponent } from '../../chrome/header';

@Component({
  selector: 'page-about-component',
  templateUrl: './index.html',
  styleUrls: ['./index.scss'],
})
export class PageAboutComponent {
}
